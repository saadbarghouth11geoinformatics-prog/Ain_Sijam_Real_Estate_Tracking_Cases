Use `project_data_v3.json` for every project. Display `comparison_clear_before_latest.jpg` as the main visual, followed by `construction_progress_3_clear_stages.jpg`. Put the five historical images in an interactive year slider and the two files in `latest_high_resolution/` inside a zoomable gallery. Never place all images in one long row. Use one large comparison per screen, clear Arabic stage labels, zoom/fullscreen controls, and a source/date strip. Sentinel-2 images have verified acquisition dates. Do not invent a capture year for Esri World Imagery. Preserve the existing Ain Sijam design and implement the changes directly.
